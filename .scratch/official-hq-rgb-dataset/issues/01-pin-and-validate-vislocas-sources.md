# 01 — 锁定并校验 Vislocas 官方源数据

**What to build:** 让数据集维护者通过一次受控的数据源准备流程，获得可审计、可复现的 Vislocas 官方源表。流程只信任固定 Zenodo 记录中通过固定摘要校验的文件，不会把当前过时的本地数据误当成权威输入。

**Blocked by:** None — can start immediately

**Status:** done

- [x] 数据源固定为 Zenodo record `10632698`，且只接受 `normalLabeled.csv`、`data_train.csv`、`data_test.csv` 三个指定产物。
- [x] 三个产物分别校验 MD5：`37dff5cc73458fe529eb860c9a2ab900`、`0236eb02e2f906282ccea4cf47a84591`、`3e9f1ddaf5e14d7a61354f0884b1f002`。
- [x] 已存在的缓存只有在摘要匹配时才会复用；摘要错误时重新获取，重新获取后仍不匹配则明确失败。
- [x] 新缓存经过校验后再原子发布，失败不会留下可被后续运行误用的半成品。
- [x] 官方源行标识缺失、重复或无法在补充源中得到唯一解释时，流程停止并给出一致性错误，不猜测或改写身份。
- [x] 自动化测试使用固定小型响应替代真实网络下载，覆盖正确缓存、错误缓存重取和重取后仍错误三条路径。
