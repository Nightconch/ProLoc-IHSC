# 07 — 端到端验收并交接数据准备流程

**What to build:** 让维护者能够用一组小型、可控夹具完整验证“固定官方源表 → 六份 URL 清单 → RGB JPEG → 六份训练 CSV”的真实操作路径，并获得足以判断是否可运行正式下载的验收证据。

**Blocked by:** 06 — 按质量层级运行六清单下载与原子发布

**Status:** done

- [x] 一次端到端测试通过两个正式命令行入口完成清单生成和六清单下载，不绕过用户实际使用的接口。
- [x] 端到端夹具同时包含官方 HQ、补充 HQ 降级、MQ/LQ、已知与未知蛋白、跨蛋白共享 URL、透明图、非 RGB 图、全白图和失败响应。
- [x] 验收确认官方 HQ 成员和顺序不变、MQ/LQ 按策略跳过失败、六份 CSV 列契约正确、所有成功图片均为可重开的三通道 RGB JPEG。
- [x] 验收确认仅 `Protein Id` 用于泄漏检查，并用共享 URL 跨 split 的合法样例证明 URL 不会触发删除或失败。
- [x] 相同输入和种子连续运行两次得到相同清单成员、顺序、文件名和审计统计。
- [x] 两个命令行入口的帮助信息可用，错误退出码和报告位置足以指导操作者定位失败。
- [x] 全部自动化测试和语法编译检查通过，且测试不会下载真实 88 MB 补充表或执行全量生产图片下载。
- [x] 对照已批准规格完成逐项审计，没有遗留 TODO、被旧测试保留的废弃 URL 冲突行为或未说明的契约偏差。

> 验证（2026-07-28）：使用本机 Conda `psl` 环境（PyTorch `2.8.0+cu129`），根目录 `tests` 7 项和 `dataset/download` 118 项测试全部通过；语法编译、两个 CLI `--help`、空白检查及废弃契约/TODO 审计均通过。测试夹具未下载真实补充表或生产图片。
> 格式验证（2026-07-28）：上述八项要求保留为八个独立的 Markdown checklist 行，现已全部勾选。
